# 회원 페이지·관리자 페이지 분리 구현

현재 웹 프로젝트에 일반 회원용 페이지와 관리자 전용 페이지를 분리해서 구현하라.

## 1. 페이지 경로

### 일반 회원 페이지

* `/`
* `/login`
* `/signup`
* `/mypage`
* `/services`
* `/notices`
* `/support`

### 관리자 페이지

* `/admin/login`
* `/admin`
* `/admin/dashboard`
* `/admin/users`
* `/admin/posts`
* `/admin/notices`
* `/admin/banners`
* `/admin/settings`

## 2. 권한 구분

회원 테이블 또는 프로필 데이터에 다음 역할을 사용한다.

```ts
role: "user" | "admin"
```

* 비로그인 사용자는 공개 페이지만 접근 가능
* 일반 회원은 마이페이지와 회원 전용 페이지 접근 가능
* 관리자는 `/admin` 이하 모든 페이지 접근 가능
* 일반 회원이 관리자 주소에 접근하면 `/` 또는 `/login`으로 이동
* 비로그인 사용자가 회원 전용 페이지에 접근하면 `/login`으로 이동

## 3. 레이아웃 분리

일반 페이지와 관리자 페이지는 레이아웃을 공유하지 않는다.

### 일반 페이지 레이아웃

* 일반 헤더
* 로고
* 카테고리 메뉴
* 로그인·회원가입
* 마이페이지
* 일반 푸터

### 관리자 페이지 레이아웃

* 관리자 전용 사이드바
* 상단 관리자 헤더
* 대시보드
* 회원 관리
* 게시물 관리
* 공지 관리
* 배너 관리
* 설정
* 관리자 로그아웃

관리자 페이지에는 일반 웹사이트의 헤더와 푸터를 표시하지 않는다.

## 4. 관리자 기능

관리자 페이지에서 다음 기능을 구현한다.

* 전체 회원 목록
* 회원 검색
* 회원 상태 변경
* 회원 정지 및 복구
* 관리자 권한 설정
* 게시물 조회 및 삭제
* 신고 게시물 관리
* 공지사항 등록·수정·삭제
* 배너 등록·수정·삭제
* 최근 가입 회원 확인
* 오늘 방문자 및 전체 회원 통계
* 관리자 활동 기록

## 5. 보안

* 화면에서 관리자 메뉴를 숨기는 것만으로 처리하지 말 것
* 서버와 API에서도 관리자 권한을 반드시 검사할 것
* 일반 회원이 관리자 API를 직접 호출하면 `403 Forbidden` 반환
* 로그인 세션이 만료되면 로그인 페이지로 이동
* 관리자 권한 여부는 클라이언트 값만 신뢰하지 말 것
* 비밀번호와 비밀키는 환경변수로 관리할 것

## 6. UI 기준

* 기존 프로젝트 디자인을 유지할 것
* 모바일·태블릿·PC 반응형으로 구현
* 관리자 화면은 표, 검색, 필터, 페이지 이동 기능을 포함
* 로딩, 빈 데이터, 오류 상태를 각각 표시
* 삭제와 정지 기능에는 확인창을 표시
* 임시 버튼이나 작동하지 않는 기능을 만들지 말 것

## 7. 작업 규칙

* 기존 기능과 파일을 함부로 삭제하지 말 것
* 현재 프로젝트의 기술 스택과 폴더 구조를 먼저 확인할 것
* 필요한 파일만 최소한으로 수정할 것
* 권한 처리 코드를 공통 함수 또는 미들웨어로 분리할 것
* 중복 코드를 만들지 말 것
* 작업 완료 후 변경한 파일 목록과 각 파일의 역할을 설명할 것
* 빌드 오류와 타입 오류가 없는지 확인할 것
